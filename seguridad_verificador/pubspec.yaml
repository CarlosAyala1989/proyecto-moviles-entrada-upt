name: seguridad_verificador
description: Aplicación de validación de ingresos para el personal de seguridad UPT.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  cliente_api_upt:
    path: ../paquetes/cliente_api_upt
  flutter:
    sdk: flutter
  geolocator: ^14.0.3
  mobile_scanner: ^7.4.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
